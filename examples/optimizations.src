/*
  Demo focus:
  - constant folding
  - constant propagation
  - dead temporary elimination
  - constant-false loop simplification
*/

int main() {
    int x;
    int y;

    x = 2 + 3;
    y = x + 0;

    while (1 < 0) {
        y = y + 99;
    }

    return y;
}
